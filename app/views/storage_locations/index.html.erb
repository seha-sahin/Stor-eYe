<body class="bg-color">
  <div class="container main-box mt-3">
    <div class="row col-12">
      <h1 class="col-10">Storages</h1>
      <%= link_to "New Storage", new_location_path, class: "btn-create col-2 mb-3 mt-3" %>
    </div><div class="container">
  <h1 class="text-center mb-4"></h1>
  <div class="row justify-content-center">
    <div class="col-12">
      <table class="table container txt-s">
        <thead class="table-title py-3">
          <tr>
            <th>Name</th>
            <th>Address</th>
            <th>Cap.</th>
            <th>T°</th>
            <th>Restaurant</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @storage_locations.each do |storage_location| %>
            <tr>
              <td><%= storage_location.name %></td>
              <td><%= storage_location.address %></td>
              <td><%= storage_location.capacity %></td>
              <td><%= storage_location.temperature %></td>
              <td><%= storage_location.restaurant.name %></td>
              <td>
              <%= link_to "", storage_location_path(storage_location), class: "fas fa-search me-3" %>
              <%= link_to "", edit_storage_location_path(storage_location), class: "fas fa-edit me-3" %>
              <%= link_to "", storage_location_path(storage_location), method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this storage?" }, class: "fas fa-trash-alt" %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>
</body>
<body>
